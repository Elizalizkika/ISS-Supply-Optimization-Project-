document.addEventListener('DOMContentLoaded', function(){

    let predictionsForm = document.getElementById('predictions-form')
    predictionsForm.addEventListener('submit', function(event) {
        //must prevent form form being submitted 
        let selectedDate = document.getElementById('date').value;
    })
})